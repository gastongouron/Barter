<%= simple_form_for(swap) do |f| %>

  <%= f.input :name %>
  <%= f.input :description %>
  <%= f.input :location %>
  <%= f.input :start %>

  <%= f.button :submit, class: 'btn-primary' %>

<% end %>